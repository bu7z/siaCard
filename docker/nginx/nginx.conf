server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # optional: API reverse proxy, falls du nicht mit absoluten URLs arbeitest
  # location /api/ {
  #   proxy_pass http://backend:8000;
  # }
}
